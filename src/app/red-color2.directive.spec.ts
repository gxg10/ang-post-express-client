import { RedColor2Directive } from './red-color2.directive';

describe('RedColor2Directive', () => {
  it('should create an instance', () => {
    const directive = new RedColor2Directive();
    expect(directive).toBeTruthy();
  });
});
